Canonical source for PW lesson descriptions: docs/PW_ Videobeschreibungen Circle Modul 1-12(3).md (contains Module 1 Lesson 1 through Module 12 Lesson 2). Implementation policy: manually replace content/lessons/modul-XX/*.md descriptions fully, keep image placeholders, apply grammar/style improvements, preserve slugs; Markdown overrides CSV. Rendering: VideoBody renders Markdown via components/RichText.tsx with sanitization; module overview uses libs/formatText.ts for teaser. Links inline; no separate resources section in VideoBody.

CRITICAL image placeholder rule: scripts/validate-images.mjs is the source of truth for allowed image counts per module (MODULE_IMAGES inside that script). Do NOT expand libs/imageRefs.ts arrays to bypass validation. All ![][imageN] placeholders in lesson MD must use indices <= the available count defined in scripts/validate-images.mjs for that module (e.g., modul-07 allows 7 images only). If source doc uses higher indices, remap placeholders to available indices OR, with explicit approval, update validator and ensure real CDN assets exist.

Build/QA workflow: for each wave, run node scripts/validate-images.mjs, then npm run lint, then npm run build; check lesson and module pages. Video meta import: skip unless content/videos/courses.json exists or user asks to configure it; prefer in-lesson MD descriptions for now.
